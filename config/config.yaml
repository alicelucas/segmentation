train: False

save_dir: .

input_size: 512

#Testing config
test_filepath: 2.012.png #data/maddox/images/x.018.png
test_save_dir: experiments/JJ/
use_saved_model: True #If testing, whether to evaluate on random model or on pre-trained model (saved unet.h5)
model_filepath: experiments/JJ #Path to saved model file

#Training config
train_image_dir: data/maddox/images
train_mask_dir: data/maddox/masks
augmentation: True
dropout: True
validation_percentage: 0.2 # 0.2
lr: 0.001
batch_size: 4
weight_class: [1, 5, 3] #What weiwhy ght to use for the minority border class
crop_border: 0 #Amount to crop on each side when training
epochs: 200
optimizer_name: Adam #E.g. SGD, Adam, RMSProp
saved_model_name: unet.h5

